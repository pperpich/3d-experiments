import type { ExperimentMeta } from '../../core/types'

export default {
  title: 'Amazing Grace',
  summary: 'The soft cluster as an instrument: retuned to the hymn’s six notes plus a drone, played by invisible fingers. Play along while it plays.',
  renderer: 'webgl',
  views: [{ pos: [0, 3.4, 3.9], target: [0, 0.1, 0] }],
} satisfies ExperimentMeta
